# Dépendances d'exécution du pipeline. Miroir de `[project.dependencies]` dans
# `pyproject.toml`, pour qui préfère `pip install -r`.
#
# Rien d'autre n'est requis : le reste du pipeline n'utilise que la bibliothèque
# standard. En particulier `unidecode` n'est PAS une dépendance — la
# désaccentuation du slug passe par `unicodedata.normalize('NFKD', …)`.
beautifulsoup4>=4.12
# `lxml` is not optional despite being a *parser* for beautifulsoup4: the parser
# is named explicitly at every call site (`BeautifulSoup(html, "lxml")`), so its
# absence is a `FeatureNotFound`, not a fallback onto `html.parser`. It was
# missing here while installed on the authors' machines — the suite passed
# locally and failed on a clean environment.
lxml>=5.0
jsonschema>=4.20
requests>=2.31

# `pf_dons.data_loader.load_raw` lit `data/dons/Dons.csv` via pandas.
pandas>=2.0

# Tests : pytest seul, sans plugin.
pytest>=8.0
